let PrimeiroNum = 0
    let SegundoNum = 1
    c = 0
    let escolha = 144


    while (c <= 10) {
    
        fibo = Number(SegundoNum + PrimeiroNum)
        PrimeiroNum = SegundoNum
        SegundoNum = fibo
        c++
        console.log (fibo)
    }
        
    /*if (fibo == escolha){
        console.log (`O numero ${escolha} pertece a sequencia Fibonnaci`)
    } else{
        console.log (`Desculpe, mas o número ${escolha} não pertence a sequencia Fibonnaci`)
    }*/
